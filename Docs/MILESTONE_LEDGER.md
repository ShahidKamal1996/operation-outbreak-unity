# OPERATION OUTBREAK — MILESTONE LEDGER

> **PROVENANCE (2026-08-14):** This ledger was absent from the repository at the start of
> Milestone 1P and was recreated from `git log` (commit subject + SHA + date) plus the
> current repository state. Statuses marked IMPLEMENTED are backed by landed commits.
> Anything a commit cannot prove is marked UNKNOWN or POSTPONED instead of being guessed.

## Legend

- **IMPLEMENTED** — work landed on an Arena working branch and its commit(s) are below.
- **IMPLEMENTED — AWAITING MANUAL UNITY QA** — Arena implementation complete; human
  Unity QA required before the milestone is accepted (never self-verified).
- **VERIFIED** — the project owner's local Unity QA passed and the milestone was
  accepted. Arena implementation alone is never sufficient for this status.
- **POSTPONED** — work paused by milestone brief; not part of the active line.

## Ledger

| Milestone | Title | Status | Date | Evidence |
|---|---|---|---|---|
| 1A | Combat lane + camera foundation | IMPLEMENTED | 2026-08-11 | `348a972` |
| 1A.1 | Refine camera composition | IMPLEMENTED | 2026-08-11 | `47001cb` |
| 1B | Player movement prototype | IMPLEMENTED | 2026-08-11 | `c7fc1f8` |
| 1C | Basic shooting foundation | IMPLEMENTED | 2026-08-11 | `9cf757d` |
| 1D | First zombie combat prototype | IMPLEMENTED | 2026-08-11 | `a6e9273` |
| 1D.1 | Fix zombie player damage | IMPLEMENTED | 2026-08-11 | `2410038` |
| 1D.2 | Implement player death state | IMPLEMENTED | 2026-08-11 | `e47f5d5` |
| 1E | Basic player health HUD | IMPLEMENTED | 2026-08-11 | `4c36c4a` |
| 1E.1 | Fix health bar fill | IMPLEMENTED | 2026-08-11 | `8a10eef` |
| 1F | Controlled zombie waves | IMPLEMENTED | 2026-08-11 | `5c104fc` |
| 1F.1 | Fix zombie crowd separation | IMPLEMENTED | 2026-08-11 | `b4fc178` |
| 1G | Automatic enemy targeting | IMPLEMENTED | 2026-08-11 | `2d4b87f` |
| 1H | Combat feedback | IMPLEMENTED | 2026-08-11 | `1418b5c` |
| 1H.1 | Fix combat feedback visibility | IMPLEMENTED | 2026-08-11 | `d1ca9f5` |
| 1I | Game over and restart flow | IMPLEMENTED | 2026-08-11 | `b3c4120` |
| 1I.1 | Remove obsolete Unity API warning | IMPLEMENTED | 2026-08-11 | `503870e` |
| 1J.1 | Weapon upgrade foundation | IMPLEMENTED (feature POSTPONED) | 2026-08-11 | `94e914f` |
| 1J.2A | Visible upgrade gate pair | IMPLEMENTED (feature POSTPONED) | 2026-08-11 | `a9f5796` |
| 1J.2B | Gate labels + player triggers / readability passes | IMPLEMENTED (feature POSTPONED) | 2026-08-11 | `42afe3f`, `8e8163c`, `df431e8`, `32f6389`, `65c2a29` |
| 1J.3 | Gate choice application and one-choice locking | IMPLEMENTED (feature POSTPONED) | 2026-08-12 | `57468b0` |
| 1K | Mission complete flow | IMPLEMENTED | 2026-08-12 | `9d66804` |
| 1L | Second upgrade gate pair | SUPERSEDED by 1L-R | 2026-08-12 | `490a584` |
| 1L-R | Replace upgrade gates with timed upgrade pickups | IMPLEMENTED | 2026-08-12 | `65db6a8` |
| 1L-R.1 | Tighten upgrade pickup sequence timing | IMPLEMENTED | 2026-08-12 | `c561559` |
| 1L-R.2 | Randomise upgrade order and pickup spawn positions | IMPLEMENTED | 2026-08-12 | `014e2f8` |
| 1M | First structured mission flow (3 sections) | IMPLEMENTED | 2026-08-12 | `7ed7483` |
| 1N | Enemy variety foundation (Runner) | IMPLEMENTED | 2026-08-12 | `aaa338c` |
| 1N.1 | Archetype-specific spawn distance offset | IMPLEMENTED | 2026-08-12 | `ea30642` |
| 1N.2 | Per-archetype minimum spawn standoff | IMPLEMENTED | 2026-08-13 | `e81c60f` |
| 1N.2-R | Correct offset cancellation semantics + pickup window fixture | IMPLEMENTED | 2026-08-13 | `d6a62a4` |
| 1O | Automated gameplay diagnostics + EditMode test foundation | IMPLEMENTED | 2026-08-12 | `c76959c` |
| 1O-R | Correct diagnostic false failures + audit runner spawn offset | IMPLEMENTED | 2026-08-13 | `0102997` |
| 1O.5 | Real player character integration (Carl visual + animation bridge) | IMPLEMENTED | 2026-08-13 | `4349004`, `b6bc395` |
| — | Mobile rendering baseline | IMPLEMENTED | 2026-08-14 | `35f8481` |
| — | Gameplay post-processing baseline | IMPLEMENTED | 2026-08-14 | `f43af78` |
| **1P** | **Weapon & combat feel foundation** | **VERIFIED** | 2026-08-14 → QA 2026-08-16 | commits on `arena/milestone-1p-weapon-combat-feel` (see git log) — owner QA: EditMode 109/109 PASS, gameplay diagnostics PASS (0 failures), recording reviewed |
| **1P.5** | **Toon Soldier visual integration** | **VERIFIED** | 2026-08-16 → owner QA 2026-08-17 (fix 12B: Shoot Layer state machine persistence — 2026-08-17) | commits on `arena/milestone-1p5-toon-soldier-integration` — presentation-only swap with 12 QA fixes (controller rebuild tool, grounding −1, presentation aim, hand-cluster muzzle measurement, follow-muzzle architecture, layered shooting with upper-body mask); EditMode 137/137; generated assets committed at `5f14ebb` |
| **1Q** | **Production enemy foundation (Basic Infected)** | **VERIFIED** | 2026-08-17 → owner manual Unity QA PASSED 2026-08-19 (EditMode 205/205) | commits on `arena/milestone-1q-production-enemy-foundation` — enemy presentation bridge, OO_BasicInfected.controller (idle/walk/attack/death; run reserved), StylizedZombie visual setup tool, death presentation window; +7 regression tests (144 total). QA fix 1 / Bug 4: walk cadence synced via a Walk-only LocomotionSpeedMultiplier parameter (reference derived from the clip's averageSpeed; gameplay 2.5 u/s unchanged; Attack/Death timing untouched); +3 tests (147 total). QA fix 1B / Bugs 1-3: asset-derived grounding offset (~-1.005 m), animation-safe restart-free hit flash with legacy punch gated to the prototype, clip-derived death window (length + 0.3 s); +4 tests (151 total). QA fix 1C: CS0019 compile fix - AnimationClip.averageSpeed is a Vector3; cadence reference now uses .magnitude. QA fix 2: deterministic FBX-derived grounding (-1.005, bounds measurement removed), hit-flash cooldown (no fire-rate strobe), direct Death crossfade (shared state name); +3 tests (153 total). QA fix 3: deterministic death entry via Animator.Play (layer 0, time 0) + source-controlled OO URP zombie materials (vendor built-in shader was magenta on clean clones) assigned to all LODs by the setup tool; +4 tests (158 total). QA fix 4: full-path death state targeting (Animator.Play with 'Base Layer.Death' hash, shared constants, base machine name pinned/validated) + editor isolation diagnostic; +3 tests (161 total). QA fix 5: one-shot death presentation latch (ShouldStartDeathPresentation), diagnostic never re-Plays an active Death state (logs normalized-time progression instead), Death state pinned to speed 1 / no self-re-entry; +3 tests (164 total). QA fix 6: death-only corpse grounding - late death-pose measurement + smooth ProductionVisual Y settle (standing offset unchanged, fallback offset serialized); +4 tests (168 total). QA fix 7: world-space grounding delta (corpse measured in WORLD Y, lane derived from root height, refinement pass at clip end, console log per pass) + collider lifecycle (root colliders disabled at death, snapshot restored on reuse); +5 tests net (172 total). QA fix 8: monotonic downward-only death settle (target clamp vs standing ceiling + previous target, blend re-asserts min, upward corrections discarded); +4 tests (176 total). QA fix 9: deactivation waits for presentation completion (bridge reports clip finished + grounding settled within 0.015 tolerance; snap-to-target; production hold 0.15 s; safety timeout 4 s; prototype fallback unchanged); +3 tests (179 total). QA fix 10: post-death sinking removed — corpse grounding is now part of the death animation itself (final grounded death Y measured ONCE at setup from the near-final death pose and serialized onto the bridge; runtime blends standingY -> finalDeathGroundedY as a smoothstep of Death normalized time 0.25 -> 0.85 with a per-frame downward-only clamp; obsolete runtime sample/refinement/MoveTowards settle machinery REMOVED, reflection-pinned; grounding completes at 0.85, before the 0.999 clip-finish gate); +5 tests net (184 total). QA fix 11: final pose calibration — the 0.95 calibration sample was slightly too early (clip still moves vertically through the tail), so the serialized Y left the corpse a little above the road; calibration now samples the true near-end pose at t=0.999 (1.0 minus tiny epsilon) with a vertical profile log (0.95/0.99/0.999) plus a small configurable downward contact margin (0.02, clamped [0, 0.05]) applied at runtime; +3 tests (187 total). 1Q FINAL: hybrid animation -> ragdoll death (approved production direction, replaces animation-only corpse grounding) — Death clip plays as a 0.30 s animation lead-in (0.25-0.40 band), then the skeleton hands off to a lightweight mobile ragdoll (11 major humanoid bones from the StylizedZombieAvatar; primitive colliders; ConfigurableJoints with symmetric hard limits; kinematic bodies + disabled colliders while alive; full reuse reset restoring authored poses/velocities/kinematic/colliders/Animator; animation grounding window zeroed so corpse-Y correction never fights physics); new EnemyRagdoll runtime + Set Up Basic Infected Ragdoll editor tool (auto-runs inside the production visual setup); +11 tests (198 total). Hybrid Ragdoll QA fix 1: stabilize the production ragdoll — capsules now aligned to each bone's REAL child direction (per-bone RagdollCollider holders, no fixed local-Y assumption), conservative per-group radii (0.05-0.17), self-collision OFF via the committed OO_Ragdoll layer (TagManager layer 8 + runtime Physics.IgnoreLayerCollision), anatomical per-axis ConfigurableJoint limits (hinge-like elbows/knees ±15 twist/±10 lateral, controlled shoulders/hips, modest spine, controlled head), stabilized handoff (velocities zeroed -> Animator off -> colliders on -> prepared-gate -> bodies freed hips-first), maxAngularVelocity 7 + angularDrag 0.4, rebalanced masses (connected ratios <= 2.4), post-generation validation + read-only debug menu; +7 tests (205 total) |
| 1R | Enemy animation & death foundation | **VERIFIED through the extended 1Q delivery** (roadmap reconciliation — the 1R scope was delivered and QA-verified inside the 1Q cycle; NOT rebuilt separately) | — | verified 1Q assets at `0ca4d76` |
| **1S** | **Enemy variant architecture** | **VERIFIED** | 2026-08-19 → owner real-Unity QA 2026-08-20 (EditMode 219/219; Validate Enemy Archetypes PASS; Basic/Runner/Toon Soldier/ragdoll/reuse verified; console clean) | commits on `arena/milestone-1s-enemy-variant-architecture` — data-driven `EnemyArchetypeDefinition` ScriptableObjects (Basic + Runner assets under Resources/EnemyArchetypes), runtime `EnemyArchetypeRegistry` (stable ids, duplicate detection, default-to-Basic), `EnemyArchetypeApplication` (stats + presentation applied to the SHARED ZombieController/EnemyAnimationBridge at spawn; no per-variant controller classes - reflection-pinned), Basic migration with the VERIFIED values (health 3 / speed 2.5 / damage 1 / interval 1 / range 1.25 / separation 1.1-1.5 / cadence 0.29091793 / Walk profile / prefab default controller), Runner preparation (speed 4.5, health 2, Run profile -> reserved zombie run clip, controller authored by the shared tool into Resources), spawner seam (SpawnEnemy(id/definition/SpawnEnemyWithDefinition) - the current 1N mission path untouched), shared prefab strategy (ONE Zombie_Prototype for all production variants), shared hybrid ragdoll (requiresRagdoll validated against the shared prefab), editor validation + debug spawn menus; +11 tests (216 total). 1S QA fix 1: test-fixture fixes only (no production code) - the unknown-id fallback test now registers LogAssert.Expect for the registry's intentional error diagnostic (proving both the diagnostic AND the Basic fallback), and the shared-controller fixture adds the gameplay CapsuleCollider first so AddComponent<ZombieController> satisfies the untouched RequireComponent(Collider) rule; count unchanged (216 total). 1S QA fix 2: Unity 6 kinematic-velocity warnings fixed - both EnemyRagdoll velocity-zeroing paths wrote linear/angular velocity while bodies were kinematic (activation zeroed BEFORE the flip, reuse zeroed AFTER re-kinematic-ing), which Unity 6 logs and discards; all velocity assignments now live in ONE guarded site (ZeroVelocitiesWhereLegal with the per-body IsVelocityWriteAllowed(!isKinematic) gate), called AFTER the kinematic flip at activation (same frame, pre-simulation - stabilization now genuinely effective) and BEFORE re-kinematic-ing at reuse; warnings not suppressed; parent-before-child order, collider lifecycle, OO_Ragdoll policy, death timing and pooling untouched; +2 tests (218 total). 1S QA fix 3: Unity 6 obsolete API clean-console fix - the debug-spawn utility's only project-wide FindFirstObjectByType call (CS0618, instance-ID ordering dependency) replaced with the semantically equivalent FindAnyObjectByType (the probe needs ANY spawner; the scene has exactly one); no production behaviour change, count unchanged (218 total). 1S QA fix 4: test-only LogAssert.Expect exact-match repair - Unity's string overload compares the FULL Debug.LogError message, so the fix #1 partial-string expectation never matched; the expectation now carries the production message verbatim (byte-exact) and the test ends with LogAssert.NoUnexpectedReceived (proving the expectation matched); production diagnostic unchanged, count unchanged (218 total) | 1S QA fix 5: ToonSoldier_Player Shoot Layer persistence harden + regression guard - real Unity reported "Statemachine for layer 'Shoot Layer' is missing"; the committed controller was verified to already carry the persisted Shoot Layer state machine (1P.5 fix 12B, af83054) and no 1S commit touched it, so the fix is at SOURCE for the remaining gap: the rebuild tool now hides the locomotion BlendTree sub-asset (HideInHierarchy, matching the Shoot Layer machine), the committed controller's BlendTree m_ObjectHideFlags was surgically corrected 0->1 (canonical sub-asset serialization), and a NEW regression test (CommittedControllerCarriesValidPersistedShootLayerWithoutRebuild) loads the COMMITTED asset without rebuilding and proves both layer state machines, the persisted Shoot Layer sub-asset, the upper-body mask, the Gunplay state/motion, the locomotion BlendTree, and that the Shoot Layer state machine survives a save/force-reimport/reload round-trip; production behaviour unchanged; +1 test (219 total) |
| **1T** | **Mission definition foundation** | **VERIFIED** | 2026-08-20 → owner real-Unity QA 2026-08-20 (EditMode 238/238; Validate Mission Definitions PASS; Validate Enemy Archetypes PASS) | commits on `arena/milestone-1t-mission-definition-foundation` (branched from verified `c8fb492`) — data-driven `MissionDefinition` ScriptableObject (identity: stable missionId/number/chapter/display name; ordered `MissionSection` list with stable sectionId + corridor values + composition; `EnemyCompositionEntry { archetypeId, count }` using the 1S STABLE ids); derived queries only (SectionCount / TotalEnemyCount / GetSection / GetArchetypeCount - no duplicated stored totals); committed `Mission_01.asset` reproduces the VERIFIED mission byte-equivalently (3 sections, 12 enemies, 9 Basic + 3 Runner, real distribution 3 / 3+1 / 3+2); runtime ownership unchanged (MissionDefinition = config only, MissionSectionController = progression only and now consumes the serialized `missionDefinition` reference, EnemySpawner = spawn requests resolved through its per-archetype library now keyed by the 1S `stableId`, EnemyArchetypeRegistry = resolution authority, ZombieController = shared gameplay authority; no Mission1/2/3Controller); fallback policy = loud actionable `[1T]` error + verified prototype mission built in memory (no unpredictable partial gameplay); editor validation `Tools > Operation Outbreak > Validate Mission Definitions` (empty id, invalid numbers, zero sections, null/duplicate section ids, empty composition, null/empty/unknown archetype ids, non-positive counts, impossible progression); authoring = Create > Operation Outbreak > Mission Definition (no C#); +18 tests (`MissionDefinitionTests.cs`); `MissionStructureTests.cs` migrated to the `MissionDefinition.MissionSection` model (same 10 tests); expected total 237/237. 1T QA fix 1 (2026-08-20): the 1S-generated Runner locomotion controller was never committed, so a fresh checkout failed `Validate Enemy Archetypes` with "EnemyArchetype_Runner: locomotion controller missing at .../OO_Runner.controller" — now committed `OO_Runner.controller` + `.meta` (the same asset the `Rebuild Runner Animator Controller` workflow produces: identical to the verified Basic controller except the locomotion state plays the reserved zombie RUN clip, guid `f2701a35…`, internal clip fileID `-203655887218126122`); no validator weakened, no fallback added; +1 regression test `CommittedRunnerControllerExistsAndIsValidWithoutRebuild` (loads the committed asset without any rebuild); expected total 238/238 | 1T QA fix 2 (2026-08-20): the null-composition validation test fixture was fixed (test-only) - `CollectProblems` already rejected null composition entries, but the test's `CreateMission` helper copied sections through `SerializedObject`, and Unity serializes plain `[Serializable]` classes by value, so a null `EnemyCompositionEntry` was materialized into a default valid instance and never reached the validator; `CreateMission` now assigns the authored sections directly (reflection) so a genuinely null entry is rejected; production validation unchanged, no new test, expected total unchanged 238/238 |
| **1U** | **Objective framework foundation** | **VERIFIED** | 2026-08-20 → owner real-Unity QA 2026-08-20 (EditMode 261/261; objective runtime + ClearAllSections verified; final section committed before completion; Mission Complete exactly once; console clean) | commits on `arena/milestone-1u-objective-framework-foundation` (branched from verified `9bd6ceb`) — reusable data-driven objective framework: `MissionObjectiveDefinition` (pure data: stable `objectiveId`, `title`, `objectiveType`, `required` flag) held in an ordered `objectives` list on `MissionDefinition` (`Objectives`/`ObjectiveCount`/`RequiredObjectiveCount`/`HasRequiredObjective`/`GetObjective(id)`); first production type `MissionObjectiveType.ClearAllSections` with required progress DERIVED from the section count (never stored); runtime evaluator `MissionObjectiveRuntime` (plain, non-serialized state: CurrentProgress/RequiredProgress/IsComplete/NormalizedProgress + `RecordSectionCleared` dedup + pure `AllRequiredObjectivesComplete` gate) driven by ONE `MissionObjectiveController` (MonoBehaviour) that subscribes to `MissionSectionController.SectionCleared` and is the single completion authority (triggers `EnemySpawner.CompleteEncounter` → `EncounterCompleted` → unchanged `MissionCompleteController`); `MissionSectionController` now publishes progress only and no longer declares victory; required objectives gate completion, optional objectives never do; fallback policy = fail loud (`[1U]` error, completion never triggered for missing/no-required objectives; committed Mission_01 always carries explicit objective data); validation extended in `MissionDefinition.CollectProblems` (null/empty/duplicate/unsupported objectives, no required objective); Mission 01 migrated with ONE required `clear_all_sections` objective (3 sections / 12 enemies / 9 Basic + 3 Runner unchanged); authoring = plain MissionDefinition asset workflow (no C#); +21 tests (`MissionObjectiveTests.cs`); expected total 259/259 | 1U QA fix 1 (2026-08-20): `MissionObjectiveController.HandleSectionCleared` was declared with one parameter while `MissionSectionController.SectionCleared` is `Action<int, MissionDefinition.MissionSection>` (two arguments), producing CS0123 at the subscribe/unsubscribe sites — the handler now accepts the event's exact two arguments verbatim (index used for progress, section payload accepted); no event-signature change, no adapter, no polling; +1 reflection regression test `ObjectiveHandlerMatchesTheRealSectionClearedEventContract` (pins the delegate type and the handler parameter list); expected total 260/260 | 1U QA fix 2 (2026-08-20): synchronous completion/reentrancy ordering fixed - the objective controller (a SectionCleared subscriber) was triggering CompleteEncounter reentrantly inside the SectionCleared dispatch, so GameplayDiagnostics (another subscriber of the same event) emitted its report before recording the final section as cleared (MIS-FINAL, 2 of 3); the handler now records progress synchronously and defers the completion evaluation to a LateUpdate boundary (end of frame), so the final section is observably committed before the single Mission Complete path fires - a deferred boundary, not an arbitrary delay, and no polling; +1 regression test `FinalSectionClearIsCommittedBeforeCompletionFires`; expected total 261/261 |
| **1V** | **Rewards & results foundation** | **IMPLEMENTED — AWAITING MANUAL UNITY QA** | 2026-08-20 | commits on `arena/milestone-1v-rewards-results-foundation` (branched from verified `e97ad199`) — static `MissionRewardDefinition` (non-negative `coins`/`supplies`, zero valid; Tech Parts NOT added) on `MissionDefinition` (`reward` field + `Reward` getter); `RuntimeWallet` (plain runtime class, long balances, negative-rejecting + saturating `Grant`, `BalancesChanged` event, NOT permanent - 2C owns save); `MissionRewardService` (the ONE reward authority, driven by `EncounterCompleted`/`Died`, grants exactly once per run via a run-scoped latch reset by `OnEnable`/scene-reload, produces immutable `MissionResultData` and raises `ResultCreated`/`RewardGranted` = the 2C SaveService seam; never declares victory); `MissionResultData` (mission id/number, success/failure, coins/supplies earned, rewards-granted flag, sections completed/total; plain runtime, never serialized into MissionDefinition); `MissionResultNavigation` seam (Retry = functional scene reload through the existing reset path; Return/Next = clean intent events + documented development fallback, no fake Base/Map); success flow extends the unchanged authority chain (sections → objectives → CompleteEncounter → EncounterCompleted → MissionCompleteController now shows Coins/Supplies + RETRY/RETURN); failure flow (GameOverController RETRY/RETURN; failed runs grant zero); validation rejects null reward + negative Coins/Supplies (zero valid, Mission 01 accepted); Mission 01 keeps `coins: 0`/`supplies: 0` (PRD introduces rewards later in Chapter 1 - no invented balancing); +23 tests (`MissionRewardTests.cs`); expected total 284/284 | 1V QA fix 1 (2026-08-20): the success-result test fixture was fixed (test-only) - production already reported the real completed-section count (the service counts the authoritative MissionSectionController.SectionCleared events before EncounterCompleted), but the test invoked HandleEncounterCompleted directly without driving those events, so its count stayed 0; `SuccessfulMissionCreatesCorrectResultData` now drives the three section clears before completion and asserts 3/3, `FailedMissionGrantsZeroCompletionRewards` asserts 2/3 on a mid-section death, and a new test `CompletedSectionCountDerivesFromSectionClearedEvents` proves the count is event-derived (not hard-coded); no production code changed; expected total 285/285 | 1V QA fix 2 (2026-08-20): Retry no longer triggers Return - the runtime-built button LABELS were TextMeshProUGUI with raycastTarget=true and oversized (1000x100) rects, so the RETURN label overlapped the RETRY button and (being rendered on top) captured its clicks and bubbled them to the RETURN button; both MissionCompleteController and GameOverController Text helpers now set raycastTarget=false on every label (labels never intercept; each button's own Image is the only clickable region, and the button rects were already non-overlapping); no gameplay/reward/objective change; +6 tests (`MissionResultUiTests.cs` builds the real controllers, invokes the real Button.onClick and asserts Retry->RequestRetry / Return->RequestReturn on both screens, one intent per click, non-raycast labels, non-overlapping buttons); expected total 291/291 |

- Gate systems (1J series): per milestone briefs, gate development is postponed and must
  not be resumed. The 1J commits above are retained in history; 1L-R superseded gates
  with timed pickups in the live gameplay path.
- Milestone 1P explicitly freezes all validated combat/mission balance values; the
  EditMode suite in `Assets/_OperationOutbreak/Tests/Editor/` pins the approved values
  (e.g. Basic 2.5 speed / 3 HP / 1 dmg, Runner 3.5 / 2 / 1, 3 sections of 3/4/5 enemies
  with 0/1/2 Runners).
- **Milestone 1P QA result (2026-08-16, project owner's local Unity run): VERIFIED.**
  Evidence: EditMode Test Runner 109/109 passed (Unity 6.5); gameplay diagnostics
  PASS — 39 checks, 34 passed, 0 failed, 5 warnings; Console 0 errors / 0 warnings
  during the accepted run; full gameplay recording reviewed and all combat-feedback
  behaviors confirmed. The pooled-feedback coroutine regression found in the first QA
  pass is RESOLVED (`2762433` activation-order fix, `a02c65b` test corrections).
- The 5 gameplay-diagnostics warnings concern existing RUNNER encounter/spawn-pressure
  behavior only. They are explicitly **NOT** Milestone 1P failures and do not affect
  1P acceptance; they remain a pre-existing tuning item for a future milestone.
- **Milestone 1P.5 (2026-08-16):** Toon Soldier replaced Carl as the ACTIVE presentation
  layer only — gameplay untouched (no movement, weapon, enemy, mission, camera or
  rendering values changed). The verified 1P status is unchanged. 1P.5 awaits the
  project owner's manual Unity QA and is NOT verified by Arena implementation alone.
